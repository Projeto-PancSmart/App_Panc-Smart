name: panc_smart
description: Sistema de Gerenciamento de Estufa Inteligente

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  provider: ^6.1.2
  fl_chart: ^1.1.1
  intl: ^0.20.2
  firebase_database: ^12.1.0
  firebase_storage: ^13.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true